# 🛡️ ENTERPRISE PROJECT SOP (v1.2)
[MANDATE: ABSOLUTE OPERATIONAL PRECEDENCE]
[VERSION: GEN 8 AGENTIC NETWORK]

## 🏗️ 1. ARCHITECTURAL MANDATES
1. **Zero Deletion:** Never delete historical logic or documentation. Modifications must be merges, refinements, or `_LEGACY` preservations.
2. **Fenced I/O:** Always separate high-speed state (`ledger.db`) from heavy-weight artifacts (`MatrixVault/Workspace`).
3. **Thermal Awareness:** Every heavy inference call must be followed by a 1:1 cooldown period. Pause all loops if Core Temp > 42°C.
4. **Memory Cap:** Maintain a system-wide footprint of < 400MB RAM to ensure Android OS stability.

## 🧪 2. SCIENTIFIC EXECUTION STANDARDS
1. **Hypothesis First:** Every filesystem modification or code mutation must be preceded by a formal hypothesis.
2. **Execution Wrapping:** Use `python3 ~/SCIENTIFIC_EXECUTOR.py "<cmd>" "<test_cmd>" "<hypothesis>"` for all operations.
3. **Empirical Validation:** Mutations are only 'Accepted' if they pass technical validation and show a positive Markov-Algebraic fitness shift.

## 🧬 3. DOCUMENTATION FIDELITY (v10.1 Standard)
Every project must maintain the following four high-fidelity documents:
- **README.md:** Standard v10.1 including Objective, Mandates, Mermaid diagrams, and ASCII Tree.
- **Blueprint.md:** Logic and dataflow architecture.
- **ROADMAP.md:** ASCII visual roadmap of milestones.
- **CHANGELOG.md:** AI-summarized versioning and milestones.

## 📡 4. NETWORK COORDINATION
1. **Swarm Trade:** Nodes must exchange Success Patterns and Entropy events hourly via the `Matrix-Coordinator`.
2. **Global Sync:** Milestone-driven GitHub synchronization is mandatory to prevent state loss.
3. **KQML-Handoff:** Computationally expensive inference should be delegated to the Laptop node via `KQML_HANDOFF.py`.

## ✅ 5. INITIALIZATION SEQUENCE
1. Run `WAKE.sh` to diagnostic health check.
2. Sync state via `Matrix-Coordinator`.
3. Launch `FOUNDRY_MASTER.py` cockpit.
4. Activate `slow_pedagogy_daemon.py` for background evolution.

---
[STATUS: SYSTEM_BIBLE_LOCKED]
[CREDITS: 100% AUTONOMOUS ALIGNMENT]
